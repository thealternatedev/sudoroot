// @ts-nocheck
IncludeSudoUtilities();

export default {
    arguments(): number {
        return 0;
    },

    name(): string {
        return "testapp";
    },

    async execute(ps: any, ...args: string[]) {
        const sudokernel = await SudoUtilities.loadLibrary("sudokernel");

        sudokernel.system_internal.shutdown()
    }
}